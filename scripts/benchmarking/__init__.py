"""Rigel full-scale simulation benchmarking package.

Subcommands:
    run      — Execute ``rigel quant`` across conditions with named configs
    analyze  — Compare tool outputs against ground truth
    status   — Show what has been run and what is pending
"""
