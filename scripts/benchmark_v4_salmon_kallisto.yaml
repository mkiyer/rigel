# Benchmark v4 supplement: salmon + kallisto only
# Reuses the same sim data, just adds salmon/kallisto quantification

genome: /Users/mkiyer/Downloads/rigel_runs/refs/human/genome_controls.fasta.bgz
gtf: /Users/mkiyer/Downloads/rigel_runs/refs/human/genes_controls.gtf.gz
outdir: /Users/mkiyer/Downloads/rigel_runs/benchmark_output_v4_salmon_kallisto

threads: 8
transcript_filter: basic

sim_dir: /Users/mkiyer/Downloads/rigel_runs/sim_ccle_hela_salmon

# Need at least one aligner so salmon/kallisto fire (they gate on first aligner)
aligners:
  oracle:
    type: oracle

# Empty rigel configs — skip rigel quantification entirely
rigel_configs: {}

salmon:
  enabled: true

kallisto:
  enabled: true

htseq:
  enabled: false

runtime:
  keep_going: true

verbose: true
