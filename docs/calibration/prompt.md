Prompt:

We are building Rigel's gDNA hyperprior (Role B) — the DNA-density prior that gently anchors the calibration solver at ambiguous nodes (third line of defense, after strand and message-propagation; it does NOT replace the solver). Read docs/calibration/dna_prior_session_resume.md first, then gdna_hyperprior_plan.md (projection = a sampling-likelihood, "what DNA level was this observation drawn from?") and enriched_mode_sensitivity_hypotheses.md. Work splits into Goal 1 (fit the DNA landscape to the oracle) and Goal 2, the real endpoint (the projection of a node's observed density → anchor μ*). Current elegant best: the unified landscape (scripts/debug/gdna_landscape_recipe.py, 1 constant, 0 special cases, EMD 0.267 cross-suite) + an asymmetric-upward projection (scripts/debug/gdna_projection.py) that recovers under-called enriched nodes (enr_recovery −0.05 → +0.25) — because pass-0 UNDER-calls enriched gDNA, bracketed between observed log10(g/E) and total log10(mass/E). All exploration runs as pure numpy on a cached substrate (no calibrate re-runs) via scripts/debug/gdna_explore_lib.py. Constraints: elegance > accuracy (toy sims; real data may break assumptions); no magic tunables without deriving them; pause & reevaluate per round; workflows OK. Then pick a next step from §6 of the resume guide.

The resume artifacts (all written)
dna_prior_session_resume.md — the full guide: starter prompt, current state, the arc/reframes, infrastructure, artifact index, open steps, owner principles, and the abandoned production gates to clean up. Indexed in the docs README.
gdna_projection.py — the elegant asymmetric-upward projection, saved permanently (was only in /tmp) and reproducing +0.266/+0.226.
Memory pointer added so a fresh session recalls this automatically.