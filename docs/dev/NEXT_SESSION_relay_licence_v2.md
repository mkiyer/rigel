# NEXT SESSION — close the zero-control gap the per-strand licence opened

    ⚠ **A DEV DOC. Nothing may cite it, and it is NOT the state.** The state is `ROADMAP.md` §0/§1, the
    rulings `DESIGN.md`, the lessons `TRAPS.md`. ⛔ When this plan is executed, DELETE this file in the
    same commit.

    Written 2026-08-18 at the end of the session that: fixed the background wall (Gamma(Σg+½, ΣE)),
    turned the relay ON, added the nascent panel axis, built the certified oracle + walker, and landed
    the PER-STRAND population licence. Two commits: `24e5deeb` and the licence commit after it.

## WHERE THE TREE IS

* `message_propagation = True`. The PER-STRAND licence (`rna_level_scale`) is LANDED: both populations
  intact ⇒ reframe by `r`; this strand intact, other changed ⇒ `r = 1` (density transfer); this strand
  changed ⇒ NO CLAIM (value + precisions zeroed). Unit gates: `test_gdna_scale_rule.py`, the three-case
  contract, breaks verified firing.
* Panel standing (`relay_pool_ab.py`, all 32, certified truth): the licence improved ALL FOUR strata vs
  the pre-licence relay (0.92 / 0.93 / 0.77 / 0.87) and put all three convicted slots ON TRUTH
  (29119: 0.9044 → 0.0028; 6049: 0.5988 → 0.0005; 12918: 0.1628 → 0.0014).
* ⛔ **THE ONE FAILED BAR: zero controls 153,662 → 430,870**, worst `g00_ss_0.50_nrna_mid_capture_on`
  62 k → 228 k. That is the next dissection, and nothing else comes before it.

## THE NEXT DISSECTION, STEP BY STEP (the loop that worked three times today)

1. `python scripts/design/calibration_oracle.py --condition gdna_g00_ss_0.50_nrna_mid_capture_on`
   (table already certified; re-run only if caches changed).
2. Rank objects by err(v2) − err(pre-licence): both arms via `HeadSwitches(rna_level_scale=…)` through
   the `importlib` patch — ⛔ `rigel.calibration.calibrate` the NAME is the FUNCTION; patch the module
   via `importlib.import_module`, and COUNT the policy constructions (an inert arm reported "identical"
   for 314 s once).
3. Read the top slots' `boundary_flags` and the delivered channels (`_uni` / `_uni_static`) under BOTH
   arms. The question: **what did the silenced arms USED to deliver there — the self-confirming echo
   (benign to lose), or an honest measurement?**
4. ⚠ The one named suspect: the GRAFT is silenced wherever a terminus and an sj share a boundary
   (~1 % of boundaries, 10,337 on the human graph — but flux-heavy). If that is the mechanism, the
   repair is to exempt the graft's MEASUREMENT stream from the no-claim zeroing (it is the destination
   population's own count, routed — not the source population's claim), and it is a few lines in both
   twins.
5. Price on all 32 per stratum + both zero controls before landing anything
   (`TRAPS: panel-before-src` — it fired twice today and was right both times).

## THE QUEUE BEHIND IT, IN ORDER

* **`transfer_var`**: switching it OFF at the crime scene read 0.0010 — better than either licence fix
  alone. Understand WHY (it damps the correct channels' precision on long chains?) before any change.
* **the 2.5 % boundary-crossing deficit** (owner: priority, not highest): every REGION class exact,
  every BOUNDARY class 2–3 % low vs `rho·E_cross`, 8/8 dense capture-off conditions, z to −13.
  Accumulator-level or opportunity-model-level; `calibration_oracle.py`'s field gate is the detector.
* **`strand_population`**: measured byte-inert on this panel (TSS/TES bits cover the flips the
  annotation expresses). Keep-or-cut is an owner call — cut is `no-speculative-code`, keep is the
  owner's stated set of population signatures.
* **`npmle.py`**: the owner believes NPMLE is retired; `DensityNPMLE.fit` still RUNS at
  `calibrate.py:591` (enrichment landscape for σ²_transfer + QC + injection substrate). Retiring it is
  real surgery with owner sign-off.
* **the VOCABULARY RENAME** (owner, 2026-08-18): `graft`/`peel`/`pin` → concept-related names, a
  dedicated effort via `rename_census.py` + `rename_identity.py`. `DESIGN.md` §0 already rules
  `graft` → SPLICE-IN. Do not rename piecemeal.

## THE DISCIPLINE THAT PAID TODAY (keep doing exactly this)

* The loop: worst condition → rank by error MASS → concentration first → flags of the top slots →
  channel bisect (`rna_imp` was named by nulling, 0.9009 → 0.0082) → hop walk → READ the code line.
* ⛔ Score verdicts over ALL live slots (`TRAPS: a-scorer-scoped-to-the-mechanisms-targets` — the scoped
  scorer sign-flipped a whole stratum today).
* ⛔ One thing varied: the v1 bundle (licence + strand predicate) cost a full panel run to un-bundle.
* ⛔ `g00` is one-sided: ANY RNA-favoring message reads as correct there, including the echo. A zero
  control improvement must name its mechanism before it counts.
* Set the accept/reject bar BEFORE the panel returns, in writing.
