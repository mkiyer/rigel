# THE THIRD POLICY — working notes

    ⚠ **A DEV DOC. Nothing may cite it, and it is NOT the state.** ⛔ Delete when it is empty.

⭐⭐ **THIS FILE HAS BEEN EMPTIED BY THE MOVE RULE, 2026-08-20, and that is the point** — a dev doc that
becomes the state is the failure `CLAUDE.md` names. Everything it held is now in a permanent home:

| what | where it went |
|---|---|
| the two strategies are ONE continuum; the recipient decides; an imputation must be weak; what pass-0 is for | `DESIGN.md` §0c.0b, §0c.0c, §0c.0d |
| the transport knob's shrinkage; the model-free enrichment ratio; the premise variance | `EQUATIONS.md` §3.5f, §3.5g, §3.5h |
| the four lessons (source-belief rescale, face totals, structural lock, per-hop premise) | `TRAPS.md` §D |
| the policy's measured standing; the reference finding; the gDNA-spectrum plan | `ROADMAP.md` §0 and §1 ranks 1/1b |
| the Stage-3 test chromosome and its structures | `TESTING.md` §0a |

⚠ **What is NOT settled and therefore has no permanent home yet** — the one open design question:

⛔ **WHETHER SPLICED FRAGMENTS ARE ENRICHED DEPENDS ON PROBE PLACEMENT, AND NO FIXED RULE CAN BE RIGHT**
(owner, 2026-08-20). A probe deliberately designed to span an exon–exon junction enriches the fragments
crossing it; a probe deep inside an exon, far from the junction, leaves them unenriched relative to the
exon body. So a policy that always rescales the spliced influx, and one that never does, are both wrong
somewhere — it has to be LEARNED from the data. ⭐ The policy currently charges the flux bound a
`(log r)²` frame variance, which is a "we do not know" treatment of exactly this and not a solution to
it. ⚠ Recorded here rather than in `EQUATIONS.md` because nothing has been derived yet.
